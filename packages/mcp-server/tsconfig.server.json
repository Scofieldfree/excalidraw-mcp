{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",

    // Server 端只需要 ES2022，不需要 DOM
    "lib": ["ES2022"],
    "target": "ES2022"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "web", "web-dist"]
}
